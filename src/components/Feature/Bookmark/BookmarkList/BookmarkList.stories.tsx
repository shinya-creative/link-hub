import type { Meta } from '@storybook/react';
// import { BookmarkForm } from './BookmarkForm';

const meta = {
    title: 'Feature/BookmarkList',
    // component: BookmarkForm,
} satisfies Meta;

export default meta;
// ↑ Storybookはこの `export default` を探していたクマ！
